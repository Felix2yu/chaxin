# 2026-08-30

## 前端重写为纯原生 JS
- 将原本 Vue 3 + TypeScript + Vite + Tailwind + Pinia 的前端整体重写为**纯 JavaScript（ES Modules，零运行时依赖，无打包器）**。
- 目录：`web/index.html`、`web/styles.css`、`web/js/{api,theme,ui,router,app}.js`、`web/js/views/{dashboard,repos,notifications,settings}.js`、`web/icons/`。
- 构建：`web/build.sh` 把静态资源原样复制到 `internal/web/dist`，由 Go `go:embed all:dist` 内嵌（后端未改动）。`make build` 先 build.sh 再 `go build`。
- 路由：hash 路由（`#/repos` 等），无需服务端配置；后端 SPA fallback 仅兜底深链。
- 设计：侧边栏 + 顶栏 + 卡片式内容，CSS 变量做明/暗主题，沿用 sky/blue 品牌色。
- 验证：`node --check` 全部通过；`go build` 通过；本地起服务后 `/`、`/js/app.js`(text/javascript)、`/styles.css`、`/icons/*`、`/repos`(SPA 兜底)、`/api/health` 均 200。
- 顺带修复：原前端仓库列表「监控状态」筛选传 `true/false` 但后端只认 `1/0`（无效），新版 api.listRepos 已改为 `1/0`。

## 修复 GitHub Actions 构建失败（提交 773cc24）
- 真因：`Test and Coverage` 中 `TestStaticSPAFallback`（internal/web/extra_test.go）断言 SPA fallback 返回含 "placeholder" 的 index.html。重写后 dist 不再提交 index.html、且 test.yml 从不构建前端（dist 仅 .gitkeep），导致 embed 无 index.html、fallback 非 200 而失败。
- 修复 1：`.github/workflows/test.yml` 在 `go test` 前加 `sh web/build.sh` 生成 dist（Docker 构建本来就跑，故成功）；并更新过时注释。
- 修复 2：`extra_test.go` 断言由脆弱的 "placeholder" 改为结构化的 "<!doctype html>"（匹配新静态 index.html，意图不变）。
- 修复 3：`.github/dependabot.yml` 移除已删除 web/package.json 的 npm 生态条目（否则 Dependabot 因无清单持续失败）。
- 本地 `go test ./...` 全绿后推送；远程 SHA 与本地 773cc24 一致，VERIFY_OK。
